---
import Icon from 'astro-icon';
import EnquiryForm from './contactForm/Form';
---

<script src="../js/enquiryModal"></script>

<div id="enquiry-modal" class="backdrop">
	<div
		role="alertdialog"
		aria-modal="true"
		aria-labelledby="modal-label"
		aria-describedby="modal-desc"
		class="card modal"
	>
		<h2 id="modal-label">Enquire online</h2>
		<button class="button outline close" data-close="enquiry-modal">
			<Icon name="ic:round-close" />
			<div class="sr-only">Close</div>
		</button>

		<div id="modal-desc" class="desc">
			<p>Please fill in the form below and we’ll get back to you shortly.</p>
			<EnquiryForm client:only="react" />
		</div>
	</div>
</div>

<style lang="scss">
	.backdrop {
		background: rgba(0, 0, 0, 0.6);
		position: fixed;
		top: 0;
		right: 0;
		bottom: 0;
		left: 0;
		z-index: 100;
		padding: var(--s-2-4);
		display: none;
		overflow-y: auto;

		&.show {
			display: flex;
			justify-content: space-around;
			align-items: start;
		}
	}

	.modal {
		max-width: var(--max-readable-width);
		width: 100%;
		position: relative;
	}

	.close {
		display: flex;
		padding: var(--s-1);
		border-radius: 200px;
		position: absolute;
		top: var(--s-1-2);
		right: var(--s-1-2);

		svg {
			min-width: var(--s-3);
			min-height: var(--s-3);
		}
	}

	.desc {
		margin-top: var(--s-2-4);
		display: grid;
		gap: var(--s-2-4);
	}
</style>
